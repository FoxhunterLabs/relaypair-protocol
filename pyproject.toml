[project]
name = "relaypair-protocol"
version = "0.1.0"
description = "Relay-agnostic, human-mediated secure pairing protocol"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
